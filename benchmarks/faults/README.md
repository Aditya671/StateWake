# Faults

The deterministic fault catalog lives in `statewake.validation_study.faults` and
covers omitted evidence, stale or malformed evidence, changed identity, missing
authorization, modified tool output, broken provenance, invalid state, partial
workspace writes, erased recovery history, and unsigned release evidence.
