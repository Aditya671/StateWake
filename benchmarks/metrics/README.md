# Metrics

Generated metrics use deterministic JSON-compatible values:

- verification coverage
- fault detection rate
- false-positive rate
- checkable property counts

Runtime and human reconstruction-time studies are future measured extensions.
