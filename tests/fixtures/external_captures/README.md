# External Integration Fixtures

These JSON files are bounded, credential-free captures used by the external-integration fixture mode. They are test fixtures, not live external-system dependencies and not production data.
