# Compatibility fixtures

This directory is reserved for immutable JSON fixtures representing released public artifact forms.

Fixtures must be named `<artifact>-v<version>.json` or `<artifact>-legacy.json` and tested through the corresponding loader/verification path. Generated fixtures should be reviewed before committing because fixture bytes are part of the compatibility contract.
